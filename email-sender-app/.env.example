# Environment Variables for Email Sender App

# Gmail Verifier API Key (from ValidKit or similar service)
GMAIL_VERIFIER_API_KEY=your_api_key_here

# Note: OAuth2 credentials are now stored per user in the database.
# Users configure their own Gmail Client ID, Client Secret, and Refresh Token via the settings page.
# Instructions for setting up OAuth2 are provided on the settings page.
